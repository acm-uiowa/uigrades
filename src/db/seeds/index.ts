import { default as seedAdminIndexes } from "./seed-admin";
import { default as seedCoursesInformation } from "./seed-courses";

const seeds = {
    seedAdminIndexes,
    seedCoursesInformation,
};

export default seeds;
